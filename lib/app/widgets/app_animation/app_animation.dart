class AppAnimations {
  // Durations
  static const Duration fast = Duration(milliseconds: 500);
  static const Duration normal = Duration(milliseconds: 800);
  static const Duration slow = Duration(milliseconds: 1100);
  static const Duration verySlow = Duration(milliseconds: 1400);

  // Delays
  static const Duration d100 = Duration(milliseconds: 200);
  static const Duration d200 = Duration(milliseconds: 400);
  static const Duration d300 = Duration(milliseconds: 600);
  static const Duration d400 = Duration(milliseconds: 800);
  static const Duration d500 = Duration(milliseconds: 1000);
}
