import 'package:flutter/material.dart';

class RecoveryPhaseView extends StatelessWidget {
  const RecoveryPhaseView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
